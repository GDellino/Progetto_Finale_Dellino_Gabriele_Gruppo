/* let navbarCustom = document.querySelector(".navbarCustom")
window.addEventListener("scroll", () => {
    let scrollValue = window.scrollY;

    if(window.innerWidth > 600){
        if (scrollValue > 20) {
            navbarCustom.style.blur = "8px"
            
        } else{
            navbarCustom.style.blur = "8px"
        }
    }
}) */